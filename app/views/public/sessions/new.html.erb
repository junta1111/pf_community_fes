<h2>ログインページ</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :'メールアドレス' %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :'ログインパスワード(６文字以上)' %><br />
    <%= f.password_field :password, autocomplete: "current-password" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "ログイン", class: "btn btn-primary" %>
  </div>
<% end %>

<div class="field">
  <h3>ゲストとしてログインする</h3>
  <p>ゲストログインでは、コミュニティページの観覧のみご利用いただけます。</p>
  <%= link_to 'ログイン',  users_guest_sign_in_path, method: :post, class: "btn btn-secondary" %>
</div>

<%#= render "public/shared/links" %>
